name: Sync Students JSON

on:
  workflow_dispatch:
  schedule:
    - cron: "0 13 * * 6"

permissions:
  contents: write

jobs:
  sync-students:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Download Students data
        run: |
          curl --fail --silent --show-error --location \
            "https://script.google.com/macros/s/AKfycbydvgjibp1ZbUbJpUxAjGpO6unRKq2U-Tuhz4B1WNMtGDHfd0DbfcHI9DPQBbY0tGUw/exec?sheet=Students" \
            --output data/students.json

      - name: Validate JSON
        run: python -m json.tool data/students.json > /dev/null

      - name: Commit changed data
        run: |
          if [ -z "$(git status --porcelain -- data/students.json)" ]; then
            echo "Students data has not changed."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/students.json
          git commit -m "Sync Students data from Google Sheets"
          git push
